var myApp = angular.module('myApp', ['ngMessages', 'ngResource']); // modules from other files (not angular.js)

myApp.controller('mainController', function($scope, $log, $filter, $resource) {
    // $filter is a angular local service
    // inject resource service in the controller function



});